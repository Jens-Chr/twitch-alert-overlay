# Public host Traefik should route for this overlay.
ALERT_OVERLAY_HOST=alerts.example.com

# Optional local fallback password for /admin/alerts. Leave empty for Authentik-only login.
ADMIN_PASSWORD=

# Trust X-authentik-* headers from Traefik ForwardAuth. Keep this true only behind the reverse proxy.
TRUST_AUTHENTIK_HEADERS=true
AUTHENTIK_REQUIRED_GROUP=stream-admins
AUTHENTIK_FORWARD_AUTH_URL=http://authentik-server:9000/outpost.goauthentik.io/auth/traefik

# Optional maximum upload size in bytes. Default: 200 MiB.
MAX_UPLOAD_BYTES=209715200

# Traefik settings. Adjust to your Traefik installation.
TRAEFIK_ENTRYPOINT=websecure
TRAEFIK_CERT_RESOLVER=letsencrypt

# External Docker network shared with n8n and Traefik.
N8N_NETWORK=n8n-network
